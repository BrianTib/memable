const User = require('./User');
const Prompt = require('./Prompt');
const Round = require('./Round');
const Score = require('./Score');
const Session = require('./Session');

module.exports = { User, Prompt, Round, Score, Session };
